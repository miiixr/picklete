extends ./../layout

block body
  h1 product
  .nd2-card
    .card-media
        img#product_image(src=product.image, alt='product')
      card-supporting-text.has-action
        h3#product_name=product.name
        p#product_descript=product.descript
        p 庫存箱數:
          span#product_stockQuantity=product.stockQuantity
        p 單價:
          span#product_price=product.price
     .card-action
        fieldset(data-role='controlgroup', data-type='horizontal')    
          a#productUpdateButton.ui-btn.ui-corner-all(href='/product/update/#{product.id}')編輯
          a#productDeleteButton.ui-btn.ui-corner-all(href='/product/delet/#{product.id}')刪除

