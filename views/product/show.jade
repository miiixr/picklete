extends ./../layout

block body
  h1 product
  .nd2-card
    .card-media
        img#product_image(src=product.image, alt='product')
      card-supporting-text.has-action
        h3#product_name=product.name
        p#product_descript=product.descript
        p 庫存箱數:
          span#product_stockQuantity=product.stockQuantity
        p 單價:
          span#product_price=product.price
        p 商品是否上架:
          span#product_price=product.isPublish
     .card-action
        fieldset(data-role='controlgroup', data-type='horizontal')
          a#productUpdateButton.ui-btn.ui-corner-all(href='/product/index')[index]
          a#productUpdateButton.ui-btn.ui-corner-all(href='/product/edit/#{product.id}')[編輯]
          a#productDeleteButton.ui-btn.ui-corner-all(href='/product/delete/#{product.id}')[刪除]
          a#productDeleteButton.ui-btn.ui-corner-all(href='/product/publish/#{product.id}')[上架]
          a#productDeleteButton.ui-btn.ui-corner-all(href='/product/unpublish/#{product.id}')[下架]
