
module.exports = {
  checkCode : async (code) => {
    try {
      return result;
    } catch (e) {
      throw e;
    }
  },

  use: async (data) => {
    let result;
    try {
      return result;
    } catch (e) {
      throw e;
    }
  }
}
